// utilizando-se de uma biblioteca instalada do repositório npm
// caso o from não possua "./", trata-se de uma biblioteca minha
import { sqrt } from 'mathjs';

console.log(sqrt(4));
// Importante: novamente, devo incluir '"type": "module",' no arquivo package.json
